{
  "name": "create-blocklet",
  "private": true,
  "version": "0.4.70",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bump-version": "zx scripts/bump-version.mjs --quiet",
    "update:deps": "pnpm -r exec npx -y taze -r -w",
    "preinstall": "npx only-allow pnpm",
    "publish-plugins": "zx scripts/publish-plugins.mjs --quiet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bumpp": "^7.2.0",
    "zx": "^7.0.8"
  }
}
