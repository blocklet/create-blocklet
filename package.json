{
  "name": "create-blocklet",
  "private": true,
  "version": "0.4.6",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bump-version": "zx scripts/bump-version.mjs --quiet",
    "update:deps": "pnpm -r exec npx taze",
    "preinstall": "npx only-allow pnpm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bumpp": "^7.2.0",
    "zx": "^7.0.7"
  }
}
